<h1>To Do List</h1>
<ul>
@foreach (var item in Model)
{
  <li>@item.GetDescription() Due on: @item.GetDate() | <a href="/items/@item.GetId()/update">Edit</a></li>
}
</ul>
<a href="/items/new">Add a new item.</a>
